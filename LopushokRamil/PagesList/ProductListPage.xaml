<Page x:Class="LopushokRamil.PagesList.ProductListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:LopushokRamil.PagesList"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProductListPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Row="0" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel Orientation="Horizontal">
                <TextBox Width="200" x:Name="SearchTb" TextChanged="SearchTb_TextChanged"/>
            </StackPanel>
        </StackPanel>
        <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3">
            <ListView Height="150" Width="500" x:Name="ProductLv" d:ItemsSource="{d:SampleData ItemCount=5}" SelectionChanged="ProductLv_SelectionChanged">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition />
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="250"/>
                                <ColumnDefinition Width="70"/>
                            </Grid.ColumnDefinitions>
                            <Image Grid.Row="0" Grid.Column="0" Grid.RowSpan="3" Source="{Binding Image}"/>
                            <StackPanel Margin="20 0 0 0" Orientation="Vertical" Grid.Column="1">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding TypeProduct.Name}"/>
                                    <TextBlock Text="|"/>
                                    <TextBlock Text="{Binding Name}"/>
                                </StackPanel>
                                <TextBlock Text="{Binding Article}"/>
                                <StackPanel Orientation="Horizontal">
                                    <!--<TextBlock Text="{Binding Materials.Name}" Width="700" TextWrapping="Wrap" Height="30"/>-->
                                    <ItemsControl ItemsSource="{Binding ProductMaterial}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Material.Name}" Margin="0,0,5,0"/>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel Orientation="Horizontal"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                    </ItemsControl>
                                </StackPanel>
                            </StackPanel>
                            <StackPanel Grid.Column="2" >
                                <TextBlock HorizontalAlignment="Right" Text="{Binding MinCost}"/>

                            </StackPanel>
                        </Grid>

                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
        <StackPanel Grid.Column="1" Grid.Row="2" HorizontalAlignment="Right">
            <TextBox x:Name="PageTb" Height="20"  Width="40" Margin="0 20 0 0" TextChanged="PageTb_TextChanged"/>
        </StackPanel>
    </Grid>
</Page>
